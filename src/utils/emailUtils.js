class emailUtils {
    constructor() {
        throw new Error("This class cannot be instantiated");
    }

    // This is where convertuserEmail to userID will be implemented, but for now we will just return the email
    static convertEmailToUserID(email) {
        return email;
    }
}