class passwordUtils {
    constructor() {
        throw new Error("This class cannot be instantiated");
    }

    // This is where the password hashing and verification functions will be implemented, but for now we will just return the password
    static hashPassword(password) {
        return password;
    }

}